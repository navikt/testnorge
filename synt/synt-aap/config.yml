apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: synthdata-arena-aap
  namespace: dolly
  labels:
    team: dolly
  annotations:
    nais.io/run-as-user: "65532" # The nonroot user.
spec:
  image: {{image}}
  liveness:
    path: "/internal/health/liveness"
    port: 8080
    initialDelay: 30
  readiness:
    path: "/internal/health/readiness"
    port: 8080
    initialDelay: 30
  prometheus:
    enabled: true
    path: /metrics
  replicas:
    min: 1
    max: 1
    cpuThresholdPercentage: 50
  resources:
    requests:
      cpu: 300m
      memory: 1024Mi
    limits:
      memory: 3072Mi
  azure:
    application:
      allowAllUsers: true
      enabled: true
      tenant: nav.no
  accessPolicy:
    inbound:
      rules:
        - application: synthdata-arena-vedtakshistorikk
        - application: testnav-oversikt-frontend
        - application: team-dolly-lokal-app
    outbound:
      rules:
        - application: logging
          namespace: nais-system
  env:
    - name: SPRING_PROFILES_ACTIVE
      value: prod
  gcp:
    buckets:
      - name: synthdata-aap-model-bucket
  ingresses:
    - "https://synthdata-arena-aap.dev.intern.nav.no"

