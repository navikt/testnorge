apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: "dolly-auth-local"
  namespace: "dolly"
spec:
  image: "europe-north1-docker.pkg.dev/nais-management-233d/nais/localauth:v1.0.0"
  port: 8080
  replicas:
    min: 1
    max: 1
  liveness:
    path: "/internal/healthz"
  readiness:
    path: "/internal/healthz"
  resources:
    limits:
      memory: "256Mi"
    requests:
      cpu: "10m"
      memory: "32Mi"
  ingresses:
    - https://dolly-auth-local.intern.dev.nav.no
  azure:
    application:
      enabled: true
      allowAllUsers: true
      tenant: nav.no
  env:
    - name: LOCALAUTH_PUBLIC_URL
      value: "https://dolly-auth-local.intern.dev.nav.no"
