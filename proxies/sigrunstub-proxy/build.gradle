plugins {
    id "dolly-proxies"
}

sonarqube {
    properties {
        property "sonar.projectKey", "testnav-sigrunstub-proxy"
        property "sonar.projectName", "testnav-sigrunstub-proxy"
    }
}

dependencies {
    implementation 'no.nav.testnav.libs:data-transfer-objects'
}
