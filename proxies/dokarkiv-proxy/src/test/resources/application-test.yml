TOKEN_X_ISSUER: dummy

spring:
  cloud:
    gcp:
      secretmanager:
        enabled: false

consumers:
  dokarkiv:
    url: http://dokarkiv-dummy.dummy.no
    cluster: dev-fss
    name: dokarkiv-dummy
    namespace: dummy
