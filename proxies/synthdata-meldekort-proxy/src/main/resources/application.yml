spring:
  application:
    name: testnav-synthdata-meldekort-proxy
    desciption: Proxy for synthdata-arena-meldekort som legger på sikkerhet.

consumers:
  synt-meldekort:
    url: https://synthdata-arena-meldekort.dev.intern.nav.no
    cluster: dev-gcp
    namespace: dolly
    name: synthdata-arena-meldekort

azure:
  trygdeetaten:
    app:
      client:
        id: ${azure.app.client.id}
        secret: ${azure.app.client.secret}
    openid:
      config:
        token:
          endpoint: ${azure.openid.config.token.endpoint}
