app:
  pdl:
    elastic:
      username: username
      password: password
    hendelse:
      apikey: apikey
  token-cache:
    grace-period-seconds: 0

management:
  endpoints:
    web:
      base-path: "/internal"
  prometheus:
    metrics:
      export:
        enabled: false