app:
  pdl:
    elastic:
      username: username
      password: password
    hendelse:
      apikey: apikey

management:
  endpoints:
    web:
      base-path: "/internal"
      exposure:
        include: "health"
  prometheus:
    metrics:
      export:
        enabled: false