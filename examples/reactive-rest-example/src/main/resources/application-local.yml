
TOKEN_X_ISSUER: nada

spring:
  application:
    # Override name
    name: reactive-rest-example-local
  h2:
    console:
      enabled: true
      path: /h2
  flyway:
    # InMemmory database
    url: jdbc:h2:mem:testdb
    username: sa
    password:
    driverClassName: org.h2.Driver