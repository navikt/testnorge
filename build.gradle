plugins {
    id 'no.nav.testnav.dependencies'
}

task build {
    dependsOn gradle.includedBuilds*.task(':build')
}

task sonarqube {
    dependsOn gradle.includedBuild("adresse-service").task(':sonarqube')
}