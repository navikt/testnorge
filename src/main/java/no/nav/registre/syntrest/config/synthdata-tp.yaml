apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: synthdata-tp
  namespace: default
  labels:
    team: synt
spec:
  image: "docker.adeo.no:5000/teamsynt/synthdata-tp"
  port: 8080
  liveness:
    path: /internal/isAlive
    initialDelay: 4
    periodSeconds: 5
    failureThreshold: 500
  readiness:
    path: /internal/isReady
    initialDelay: 4
    periodSeconds: 5
    failureThreshold: 500
  prometheus:
    path: /metrics
  replicas:
    min: 2
    max: 4
  secrets: true
  resources:
    limits:
      cpu: 500m
      memory: 8000Mi
    requests:
      cpu: 200m
      memory: 7000Mi
  ingresses:
    - "https://synthdata-tp.nais.preprod.local"