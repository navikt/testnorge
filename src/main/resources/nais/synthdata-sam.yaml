apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: synthdata-sam
  namespace: q2
  labels:
    team: synt
spec:
  image: "docker.adeo.no:5000/registre/synthdata-sam:latest"
  port: 8080
  liveness:
    path: /internal/isAlive
    initialDelay: 4
    periodSeconds: 5
    failureThreshold: 500
  readiness:
    path: /internal/isReady
    initialDelay: 4
    periodSeconds: 5
    failureThreshold: 500
  prometheus:
    path: /metrics
  replicas:
    min: 2
    max: 4
  secrets: true
  resources:
    requests:
      cpu: 200m
      memory: 20000Mi
    limits:
      cpu: 500m
      memory: 21000Mi
  ingresses:
    - "https://nais-synthdata-sam.nais.preprod.local"