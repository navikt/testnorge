apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: synthdata-arena-aap
  namespace: q2
  labels:
    team: synt
spec:
  image: "docker.adeo.no:5000/registre/synthdata-arena-aap:latest"
  port: 8080
  liveness:
    path: /internal/isAlive
    initialDelay: 4
    periodSeconds: 5
    failureThreshold: 500
  readiness:
    path: /internal/isReady
    initialDelay: 4
    periodSeconds: 5
    failureThreshold: 500
  prometheus:
    path: /metrics
  replicas:
    min: 1
    max: 2
  vault:
    enabled: true
  resources:
    requests:
      cpu: 200m
      memory: 8000Mi
    limits:
      cpu: 500m
      memory: 9000Mi
  ingresses:
    - "https://nais-synthdata-arena-aap.nais.preprod.local"