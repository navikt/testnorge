plugins {
    id "com.gradle.enterprise" version "3.7.1"
}

rootProject.name = 'arbeidsforhold-xsd'

includeBuild '../../gradlePlugins'

gradleEnterprise {
    buildScan {
        termsOfServiceUrl = "https://gradle.com/terms-of-service"
        termsOfServiceAgree = "yes"
    }
}
