apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: testnorge-aaregstub
  namespace: default
  labels:
    team: orkestrator
spec:
  image: "docker.adeo.no:5000/registre/testnorge-aaregstub:1.0.0"
  port: 8080
  liveness:
    path: /internal/isAlive
    periodSeconds: 30
  readiness:
    path: /internal/isReady
  prometheus:
    path: /internal/prometheus
  replicas:
    min: 1
    max: 2
  vault:
    enabled: true
  resources:
    requests:
      cpu: 200m
      memory: 1024Mi
    limits:
      cpu: 500m
      memory: 2048Mi
  ingresses:
    - "https://testnorge-aaregstub.nais.preprod.local"