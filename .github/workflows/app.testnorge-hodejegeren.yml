name: App testnorge-hodejegeren

on:
  push:
    paths:
      - apps/testnorge-hodejegeren/**
      - .github/workflows/app.testnorge-hodejegeren.yml

jobs:
  scan:
    name: Scan
    uses: navikt/testnorge/.github/workflows/common.sonar.yml@github-workflows-new
    with:
      working-directory: 'apps/testnorge-hodejegeren/'
    secrets:
      NAV_TOKEN: ${{ secrets.NAV_TOKEN }}
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
  build:
    name: Build
    uses: navikt/testnorge/.github/workflows/common.build-and-publish.backend.yml@github-workflows-new
    with:
      working-directory: 'apps/testnorge-hodejegeren/'
      app-name: 'testnav-hodejegeren'
    secrets:
      NAV_TOKEN: ${{ secrets.NAV_TOKEN }}
  deploy:
    name: Deploy
    uses: navikt/testnorge/.github/workflows/common.deploy.yml@github-workflows-new
    needs: build
    if: ${{ github.ref == 'refs/heads/master' }}
    with:
      resource: 'apps/testnorge-hodejegeren/nais.yml'
      app-name: 'testnav-hodejegeren'
      cluster: 'dev-fss'
    secrets:
      API_KEY: ${{ secrets.NAIS_DOLLY_DEPLOY_API_KEY }}