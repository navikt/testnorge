name: App mn-synt-arbeidsforhold-service

on:
  push:
    paths:
      - apps/mn-synt-arbeidsforhold-service/**
      - .github/workflows/app.mn-synt-arbeidsforhold-service.yml

jobs:
  scan:
    name: Scan
    uses: navikt/testnorge/.github/workflows/common.sonar.yml@github-workflows-new
    with:
      working-directory: 'apps/mn-synt-arbeidsforhold-service/'
    secrets:
      NAV_TOKEN: ${{ secrets.NAV_TOKEN }}
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
  build:
    name: Build
    uses: navikt/testnorge/.github/workflows/common.build.backend.yml@github-workflows-new
    with:
      working-directory: 'apps/mn-synt-arbeidsforhold-service/'
      app-name: 'mn-synt-arbeidsforhold-service'
    secrets:
      NAV_TOKEN: ${{ secrets.NAV_TOKEN }}
  deploy-dev:
    name: Deploy
    uses: navikt/testnorge/.github/workflows/common.deploy.yml@github-workflows-new
    needs: build
    if: ${{ github.ref == 'refs/heads/master' }}
    with:
      resource: 'apps/mn-synt-arbeidsforhold-service/config.yml'
      app-name: 'mn-synt-arbeidsforhold-service'
      cluster: 'dev-fss'
    secrets:
      API_KEY: ${{ secrets.NAIS_DOLLY_DEPLOY_API_KEY }}