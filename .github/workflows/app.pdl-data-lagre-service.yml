name: testnav-pdl-data-lagre-service

on:
  push:
    paths:
      - .github/workflows/app.pdl-data-lagre-service.yml
      - apps/pdl-data-lagre-service/**
      - libs/avro-schema/**
      - libs/data-transfer-objects/**
      - libs/kafka-config/**
      - libs/reactive-core/**
      - libs/security-core/**
      - libs/servlet-core/**
      - libs/servlet-insecure-security/**
      - libs/testing/**
      - plugins/**

jobs:
  workflow:
    uses: ./.github/workflows/common.workflow.backend.yml
    with:
      working-directory: "apps/pdl-data-lagre-service"
      deploy-tag: "#deploy-pdl-data-lagre-service"
    permissions:
      contents: read
      id-token: write
    secrets: inherit
