name: testnav-pdl-lagre-data-service

on:
  push:
    paths:
      - .github/workflows/app.pdl-lagre-data-service.yml
      - apps/pdl-lagre-data-service/**
      - libs/avro-schema/**
      - libs/data-transfer-objects/**
      - libs/kafka-config/**
      - libs/reactive-core/**
      - libs/security-core/**
      - libs/servlet-core/**
      - libs/servlet-insecure-security/**
      - libs/testing/**
      - plugins/**

jobs:
  workflow:
    uses: ./.github/workflows/common.workflow.backend.yml
    with:
      working-directory: "apps/pdl-lagre-data-service"
      deploy-tag: "#deploy-pdl-lagre-data-service"
    permissions:
      contents: read
      id-token: write
    secrets: inherit
