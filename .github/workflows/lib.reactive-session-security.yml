name: Lib reactive-session-security

on:
  push:
    paths:
      - libs/reactive-session-security/**
      - .github/workflows/lib.reactive-session-security.yml

jobs:
  scan:
    name: Scan
    uses: navikt/testnorge/.github/workflows/common.sonar.yml@master
    with:
      working-directory: 'libs/reactive-session-security/'
    secrets:
      NAV_TOKEN: ${{ secrets.NAV_TOKEN }}
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
  build:
    name: Build
    uses: navikt/testnorge/.github/workflows/common.java-build.yml@master
    with:
      working-directory: 'libs/reactive-session-security/'
    secrets:
      NAV_TOKEN: ${{ secrets.NAV_TOKEN }}