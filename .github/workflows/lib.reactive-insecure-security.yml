name: reactive-insecure-security

on:
  push:
    paths:
      - plugins/**
      - libs/reactive-insecure-security/**
      - .github/workflows/lib.reactive-insecure-security.yml

jobs:
  workflow:
    uses: ./.github/workflows/common.workflow.libs.backend.yml
    with:
      working-directory: 'libs/reactive-insecure-security'
    secrets: inherit
