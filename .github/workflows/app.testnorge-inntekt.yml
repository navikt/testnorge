name: App testnorge-inntekt

on:
  push:
    paths:
      - apps/testnorge-inntekt/**
      - .github/workflows/app.testnorge-inntekt.yml

jobs:
  scan:
    name: Scan
    uses: navikt/testnorge/.github/workflows/common.sonar.yml@master
    with:
      working-directory: 'apps/testnorge-inntekt/'
    secrets:
      NAV_TOKEN: ${{ secrets.NAV_TOKEN }}
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
  build:
    name: Build
    uses: navikt/testnorge/.github/workflows/common.build-and-publish.backend.yml@master
    with:
      working-directory: 'apps/testnorge-inntekt/'
      app-name: 'testnav-inntekt'
    secrets:
      NAV_TOKEN: ${{ secrets.NAV_TOKEN }}
