name: Lib commands

on:
  push:
    paths:
      - libs/commands/**
      - .github/workflows/libs.commands.yml

jobs:
  scan:
    name: Scan
    uses: navikt/testnorge/.github/workflows/common.sonar.yml@github-workflows-new
    with:
      working-directory: 'libs/commands/'
    secrets:
      NAV_TOKEN: ${{ secrets.NAV_TOKEN }}
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
  build:
    name: Build
    uses: navikt/testnorge/.github/workflows/common.java-build.backend.yml@github-workflows-new
    with:
      working-directory: 'libs/commands/'
    secrets:
      NAV_TOKEN: ${{ secrets.NAV_TOKEN }}