name: App testnorge-skd

on:
  push:
    paths:
      - apps/testnorge-skd/**
      - .github/workflows/app.testnorge-skd.yml

jobs:
  scan:
    name: Scan
    uses: navikt/testnorge/.github/workflows/common.sonar.yml@master
    with:
      working-directory: 'apps/testnorge-skd/'
    secrets:
      NAV_TOKEN: ${{ secrets.NAV_TOKEN }}
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
  build:
    name: Build
    uses: navikt/testnorge/.github/workflows/common.build-and-publish.backend.yml@master
    with:
      working-directory: 'apps/testnorge-skd/'
      app-name: 'testnav-skd'
    secrets:
      NAV_TOKEN: ${{ secrets.NAV_TOKEN }}
