name: App generer-arbeidsforhold-populasjon-service

on:
  push:
    paths:
      - apps/generer-arbeidsforhold-populasjon-service/**
      - .github/workflows/app.generer-arbeidsforhold-populasjon-service.yml

jobs:
  scan:
    name: Scan
    uses: navikt/testnorge/.github/workflows/common.sonar.yml@github-workflows-new
    with:
      working-directory: 'apps/generer-arbeidsforhold-populasjon-service/'
    secrets:
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
  build:
    name: Build
    uses: navikt/testnorge/.github/workflows/common.build.backend.yml@github-workflows-new
    with:
      working-directory: 'apps/generer-arbeidsforhold-populasjon-service/'
      app-name: 'generer-arbeidsforhold-populasjon-service'
  deploy-dev:
    name: Deploy
    uses: navikt/testnorge/.github/workflows/common.deploy.yml@github-workflows-new
    needs: build
    if: ${{ github.ref == 'refs/heads/master' }}
    with:
      resource: 'apps/generer-arbeidsforhold-populasjon-service/config.yml'
      app-name: 'generer-arbeidsforhold-populasjon-service'
      cluster: 'dev-gcp'
    secrets:
      API_KEY: ${{ secrets.NAIS_DOLLY_DEPLOY_API_KEY }}