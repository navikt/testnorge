name: testnav-pdl-lagre-service

on:
  push:
    paths:
      - .github/workflows/app.pdl-lagre-service.yml
      - apps/pdl-lagre-service/**
      - libs/avro-schema/**
      - libs/data-transfer-objects/**
      - libs/kafka-config/**
      - libs/reactive-core/**
      - libs/security-core/**
      - libs/servlet-core/**
      - libs/servlet-insecure-security/**
      - libs/testing/**
      - plugins/**

jobs:
  workflow:
    uses: ./.github/workflows/common.workflow.backend.yml
    with:
      working-directory: "apps/pdl-lagre-service"
      deploy-tag: "#deploy-pdl-lagre-service"
    permissions:
      contents: read
      id-token: write
    secrets: inherit
