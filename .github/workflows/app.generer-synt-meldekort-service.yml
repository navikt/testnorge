name: App generer-synt-meldekort-service

on:
  push:
    paths:
      - apps/generer-synt-meldekort-service/**
      - .github/workflows/app.generer-synt-meldekort-service.yml

jobs:
  scan:
    name: Scan
    uses: navikt/testnorge/.github/workflows/common.sonar.yml@master
    with:
      working-directory: 'apps/generer-synt-meldekort-service/'
    secrets:
      NAV_TOKEN: ${{ secrets.NAV_TOKEN }}
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
  build:
    name: Build
    uses: navikt/testnorge/.github/workflows/common.build-and-publish.backend.yml@master
    with:
      working-directory: 'apps/generer-synt-meldekort-service/'
      app-name: 'generer-synt-meldekort-service'
    secrets:
      NAV_TOKEN: ${{ secrets.NAV_TOKEN }}
  deploy:
    name: Deploy
    uses: navikt/testnorge/.github/workflows/common.deploy.yml@master
    needs: build
    if: ${{ github.ref == 'refs/heads/master' || github.ref == 'refs/heads/feature/generer-synt-meldekort-service'  }}
    with:
      resource: 'apps/generer-synt-meldekort-service/config.yml'
      app-name: 'generer-synt-meldekort-service'
      cluster: 'dev-gcp'
    secrets:
      API_KEY: ${{ secrets.NAIS_DOLLY_DEPLOY_API_KEY }}