name: App testnorge-nav-endringsmeldinger

on:
  push:
    paths:
      - apps/testnorge-nav-endringsmeldinger/**
      - .github/workflows/app.testnorge-nav-endringsmeldinger.yml

jobs:
  scan:
    name: Scan
    uses: navikt/testnorge/.github/workflows/common.sonar.yml@github-workflows-new
    with:
      working-directory: 'apps/testnorge-nav-endringsmeldinger/'
    secrets:
      NAV_TOKEN: ${{ secrets.NAV_TOKEN }}
      SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
  build:
    name: Build
    uses: navikt/testnorge/.github/workflows/common.build.backend.yml@github-workflows-new
    with:
      working-directory: 'apps/testnorge-nav-endringsmeldinger/'
      app-name: 'testnorge-nav-endringsmeldinger'
    secrets:
      NAV_TOKEN: ${{ secrets.NAV_TOKEN }}
  deploy:
    name: Deploy
    uses: navikt/testnorge/.github/workflows/common.deploy.yml@github-workflows-new
    needs: build
    if: ${{ github.ref == 'refs/heads/master' }}
    with:
      resource: 'apps/testnorge-nav-endringsmeldinger/nais.yml'
      app-name: 'testnorge-nav-endringsmeldinger'
      cluster: 'dev-fss'
    secrets:
      API_KEY: ${{ secrets.NAIS_DOLLY_DEPLOY_API_KEY }}