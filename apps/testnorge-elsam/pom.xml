<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>

    <parent>
        <artifactId>apps</artifactId>
        <groupId>no.nav.registre.testnorge</groupId>
        <version>1.0-SNAPSHOT</version>
    </parent>

    <artifactId>testnorge-elsam</artifactId>
    <version>1.0-SNAPSHOT</version>
    <packaging>pom</packaging>

    <name>testnorge-elsam</name>
    <description>Forvalter for syntetisering av sykemeldinger</description>

    <modules>
        <module>elsam-domain</module>
        <module>elsam-application</module>
    </modules>

    <properties>
        <java.version>11</java.version>
        <jacoco.version>0.8.4</jacoco.version>
        <logstash.logback.encoder.version>5.2</logstash.logback.encoder.version>
        <javax.activation.version>1.2.0</javax.activation.version>
        <jaxb.api.version>2.3.0</jaxb.api.version>
        <maven-pmd-plugin.version>3.13.0</maven-pmd-plugin.version>
        <tjenestespesifikasjon.version>1.2020.04.27-15.39-c32c8960a220</tjenestespesifikasjon.version>
    </properties>

    <dependencyManagement>
        <dependencies>
            <!-- HODEJEGEREN -->
            <dependency>
                <groupId>no.nav.registre</groupId>
                <artifactId>testnorge-consumers</artifactId>
                <version>1.10.6-RELEASE</version>
            </dependency>

            <!-- TEST -->
            <dependency>
                <groupId>org.springframework.cloud</groupId>
                <artifactId>spring-cloud-contract-wiremock</artifactId>
                <version>2.1.2.RELEASE</version>
            </dependency>
            <dependency>
                <groupId>com.ibm.mq</groupId>
                <artifactId>mq-jms-spring-boot-starter</artifactId>
                <version>2.0.0</version>
            </dependency>
            <!--LOGGING-->
            <dependency>
                <groupId>net.logstash.logback</groupId>
                <artifactId>logstash-logback-encoder</artifactId>
                <version>${logstash.logback.encoder.version}</version>
            </dependency>
            <dependency>
                <groupId>no.nav.registre.testnorge</groupId>
                <artifactId>elsam-domain</artifactId>
                <version>${project.version}</version>
            </dependency>
        </dependencies>
    </dependencyManagement>

</project>
