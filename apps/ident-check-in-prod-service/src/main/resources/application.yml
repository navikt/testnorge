AAD_ISSUER_URI: https://login.microsoftonline.com/62366534-1ec3-4962-8869-9b5535279d0b

spring:
  main:
    banner-mode: off
  application:
    version: application.version.todo #TODO Finn ut hvordan denne kan settes fra gradle
    name: ident-check-in-prod-service
    description: App som sjekker om ident finnes i prod på identer benyttet av Dolly og Orkestrator
  security:
    oauth2:
      resourceserver:
        aad:
          issuer-uri: ${AAD_ISSUER_URI}/v2.0
          jwk-set-uri: ${AAD_ISSUER_URI}/discovery/v2.0/keys
          accepted-audience: ${azure.app.client.id}, api://${azure.app.client.id}
        tokenx:
          issuer-uri: https://tokendings.dev-gcp.nais.io
          jwk-set-uri: https://tokendings.dev-gcp.nais.io/jwks
          accepted-audience: dev-fss:dolly:testnorge-ident-check-in-prod-service

springdoc:
  swagger-ui:
    url: /swagger-ui/index.html?url=/v3/api-docs/