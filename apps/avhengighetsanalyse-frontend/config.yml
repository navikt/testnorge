apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: avhengighetsanalyse-frontend
  namespace: dolly
  labels:
    team: dolly
spec:
  port: 8080
  azure:
    application:
      enabled: true
      replyURLs:
        - "https://testnav-avhengighetsanalyse.dev.intern.nav.no/login/oauth2/code/aad"
        - "http://localhost:8080/login/oauth2/code/aad"
  accessPolicy:
    outbound:
      rules:
        - application: applikasjonsanalyse-service
  liveness:
    path: /internal/isAlive
    periodSeconds: 30
    initialDelay: 30
  readiness:
    path: /internal/isReady
    initialDelay: 30
  prometheus:
    path: /internal/prometheus
  replicas:
    min: 1
    max: 1
  resources:
    limits:
      cpu: 500m
      memory: 2048Mi
    requests:
      cpu: 200m
      memory: 1024Mi
  image: {{ image }}
  ingresses:
    - "https://testnav-avhengighetsanalyse.dev.intern.nav.no"
