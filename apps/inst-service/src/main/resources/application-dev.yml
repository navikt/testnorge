azure:
  app:
    client:
      id: ${client_id}
      secret: ${client_secret}

TOKEN_X_CLIENT_ID: dev-gcp:dolly:testnav-inst-service

consumers:
  testnav-inst-proxy:
    url: https://testnav-inst-proxy.dev.intern.nav.no