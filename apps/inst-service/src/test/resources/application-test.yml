consumers:
  inst2:
    url: http://localhost:${wiremock.server.port:0}/inst2

spring:
  cloud:
    vault:
      enabled: false

azure:
  app:
    client:
      id: dummy
      secret: dummy

TOKEN_X_CLIENT_ID: test