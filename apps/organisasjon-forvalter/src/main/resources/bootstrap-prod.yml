AAD_ISSUER_URI: https://login.microsoftonline.com/62366534-1ec3-4962-8869-9b5535279d0b

organisasjon:
  navn:
    url: https://generer-navn-service.dev.intern.nav.no
    client:
      id: 92e61fa8-6fca-48e2-87b9-29d8bb6cf850
  nummer:
    url: https://organisasjon-orgnummer-service.dev.intern.nav.no
    client:
      id: cc5eab78-8ec7-4f4f-8b2f-7e6f46870a48

spring:
  flyway:
    enabled: true # Disabled by default as you should probably think twice before running Flyway-migrations
    locations: classpath:db/migration
    baseline-on-migrate: true
  datasource:
    url: jdbc:postgresql://b27dbvl023.preprod.local:5432/organisasjon-forvalter-preprod
    hikari:
      maximum-pool-size: 3
      minimum-idle: 1
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        default-schema: public
  cloud:
    vault:
      host: vault.adeo.no
      port: 443
      fail-fast: true
      connection-timeout: 15000
      read-timeout: 30000
      generic:
        enabled: false
      database:
        backend: postgresql/preprod-fss
        enabled: true
        role: organisasjon-forvalter-preprod-admin
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${AAD_ISSUER_URI}/v2.0 
          accepted-audience: ${azure.app.client.id}, api://${azure.app.client.id}