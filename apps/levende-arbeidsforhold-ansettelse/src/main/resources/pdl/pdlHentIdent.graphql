query ($ident: ID!) {
    hentIdenter(ident: $ident, historikk: true, grupper: [AKTORID, FOLKEREGISTERIDENT, NPID]) {
        identer {
            ident,
            historisk,
            gruppe
        }
    }
}