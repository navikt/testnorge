apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: synthdata-elsam
  namespace: q2
  labels:
    team: synt
spec:
  image: "docker.pkg.github.com/navikt/testnorge-syntetiseringspakker/synt_elsam:latest"
  port: 8080
  liveness:
    path: /internal/isAlive
    initialDelay: 4
    periodSeconds: 5
    failureThreshold: 500
  readiness:
    path: /internal/isReady
    initialDelay: 4
    periodSeconds: 5
    failureThreshold: 500
  prometheus:
    path: /metrics
  replicas:
    min: 1
    max: 4
  vault:
    enabled: true
  resources:
    requests:
      cpu: 200m
      memory: 18000Mi
    limits:
      cpu: 500m
      memory: 18000Mi
  ingresses:
    - "https://nais-synthdata-elsam.nais.preprod.local"
    - "https://nais-synthdata-elsam.dev.adeo.no"
    - "https://nais-synthdata-elsam.dev.intern.nav.no"