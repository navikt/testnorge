spring:
  application:
    name: testnav-pdl-person-opensearch-service
    version: 1
    description: App for å ta imot Kafka-hendelser og lagre i opensearch.
  config:
    import: "classpath:dolly-application.yml"
  security:
    oauth2:
      resourceserver:
        aad:
          issuer-uri: ${AZURE_OPENID_CONFIG_ISSUER}
          accepted-audience:  ${AZURE_APP_CLIENT_ID}, api:// ${AZURE_APP_CLIENT_ID}
  kafka:
    consumer:
      group-id: testnav-pdl-person-opensearch-service

open:
  search:
    total-fields: 2000
    index: pdl-person