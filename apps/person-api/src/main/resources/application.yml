AAD_ISSUER_URI: https://login.microsoftonline.com/62366534-1ec3-4962-8869-9b5535279d0b
CLIENT_ID: ${azure.app.client.id}

spring:
  application:
    version: application.version.todo #TODO Finn ut hvordan denne kan settes fra gradle
    name: testnorge-person-api
    desciption: API for opprettelse av personer

system:
  pdl:
    pdlTestdataUrl: https://pdl-testdata.dev.adeo.no
    pdlApi: https://pdl-api.dev.adeo.no
  tpsf:
    tpsForvalterenUrl: https://tps-forvalteren.nais.preprod.local/api

logging:
  config: "classpath:logback.xml"

sts:
  token:
    provider:
      url: ${STS_TOKEN_PROVIDER_URL}
      username: ${STS_TOKEN_PROVIDER_USERNAME}
      password: ${STS_TOKEN_PROVIDER_PASSWORD}