dolly:
  texas:
    url:
      token: http://localhost:3000/api/v1/token

spring:
  config:
    import: "sm://"

consumers:
  generer-navn-service:
    url: "https://generer-navn-service.intern.dev.nav.no"
