ibm:
  mq:
    channel: ${CHANNEL_NAME}
    queue-manager: ${IBM_MQ_QUEUE_MANAGER}
    hostname: ${IBM_MQ_HOSTNAME}
    port: ${IBM_MQ_PORT}
    user: ${IBM_MQ_USERNAME}
    password: ${IBM_MQ_PASSWORD}

spring:
  main:
    banner-mode: off
