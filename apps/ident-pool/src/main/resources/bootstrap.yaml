spring:
  cloud:
    vault:
      host: vault.adeo.no
      port: 443
      fail-fast: true
      connection-timeout: 15000
      read-timeout: 30000
      kv:
        enabled: false
      database:
        backend: postgresql/preprod-fss
        enabled: true
        role: ident-pool-preprod-admin