AAD_ISSUER_URI: https://login.microsoftonline.com/62366534-1ec3-4962-8869-9b5535279d0b

spring:
  application:
    version: application.version.todo
    name: testnav-tilgang-service
    desciption: API for hente tilganger for personer
  security:
    oauth2:
      resourceserver:
        aad:
          issuer-uri: ${AAD_ISSUER_URI}/v2.0
          jwk-set-uri: ${AAD_ISSUER_URI}/discovery/v2.0/keys
          accepted-audience: ${azure.app.client.id}, api://${azure.app.client.id}
        tokenx:
          issuer-uri: https://tokendings.dev-gcp.nais.io
          jwk-set-uri: https://tokendings.dev-gcp.nais.io/jwks
          accepted-audience: ${ACCEPTED_AUDIENCE}
  jackson:
    serialization:
      write_dates_as_timestamps: false

altinn:
  api:
    url: https://tt02.altinn.no
    key: ${ALTINN_API_KEY}
  service:
    code: 5748
    edition: 1