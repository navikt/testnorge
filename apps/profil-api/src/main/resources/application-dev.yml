azure.app.client.id: ${client_id}
azure.app.client.secret: ${client_secret}
spring:
  security:
    oauth2:
      resourceserver:
        tokenx:
          accepted-audience: dev-gcp:dolly:testnorge-profil-dev