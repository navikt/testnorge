
export const REQUEST_ERROR = 'REQUEST_ERROR'

export const runningE2ETest = () => {
    // @ts-ignore
    return window?.isRunningTest
}

export default class LevendeArbeidsforholdRequest {
    static get(url: string, headers: Record<string, string> = {}) {
        return
    }
}