import styled from 'styled-components'

export const DatepickerWrapper = styled.div`
	display: flex;
	flex-wrap: wrap;
	&&&& {
		button {
			position: absolute;
			top: 5px;
			right: 5px;
			margin-right: 0;
		}
	}
`
