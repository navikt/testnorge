spring:
  datasource:
    url: jdbc:postgresql://b27dbvl007.preprod.local:5432/udi-stub
    hikari:
      maximum-pool-size: 3
      minimum-idle: 1
    driver-class-name: org.postgresql.Driver
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        default-schema: public
  cloud:
    vault:
      host: vault.adeo.no
      port: 443
      database:
        backend: postgresql/preprod-fss
        enabled: true
        role: udi-stub-admin
      kv:
        enabled: false
