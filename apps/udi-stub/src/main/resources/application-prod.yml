spring:
  datasource:
    url: jdbc:postgresql://b27dbvl007.preprod.local:5432/udi-stub
    hikari:
      maximum-pool-size: 3
      minimum-idle: 1
      jdbc-url: jdbc:postgresql://b27dbvl007.preprod.local:5432/udi-stub
      driver-class-name: org.postgresql.Driver
  cloud:
    vault:
      host: vault.adeo.no
      port: 443
      fail-fast: true
      connection-timeout: 15000
      read-timeout: 30000
      database:
        backend: postgresql/preprod-fss
        role: udi-stub-admin
        enabled: true