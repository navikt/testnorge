spring:
  datasource:
    url: jdbc:postgresql://b27dbvl007.preprod.local:5432/udi-stub
    hikari:
      maximum-pool-size: 3
      minimum-idle: 1
      jdbc-url: jdbc:postgresql://b27dbvl023.preprod.local:5432/udi-stub-dev
      driver-class-name: org.postgresql.Driver
  cloud:
    vault:
      host: vault.adeo.no
      port: 443
      database:
        backend: postgresql/preprod-fss
        enabled: true
        role: udi-stub-admin