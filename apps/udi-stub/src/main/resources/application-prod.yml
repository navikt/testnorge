spring:
  datasource:
    url: jdbc:postgresql://dev-pg.intern.nav.no:5432/udi-stub
    hikari:
      maximum-pool-size: 3
      minimum-idle: 1
      jdbc-url: jdbc:postgresql://dev-pg.intern.nav.no:5432/udi-stub
      driver-class-name: org.postgresql.Driver
  cloud:
    vault:
      database:
        backend: postgresql/preprod-fss
        role: udi-stub-admin
        enabled: true