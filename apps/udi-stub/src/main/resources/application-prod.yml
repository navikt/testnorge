spring:
  datasource:
    url: jdbc:postgresql://b27dbvl007.preprod.local:5432/udi-stub?autoReconnect=true&useSSL=false
    hikari:
      maximum-pool-size: 3
      minimum-idle: 1
      connection-timeout: 30000
      initialization-fail-timeout: 10000
      jdbc-url: jdbc:postgresql://b27dbvl007.preprod.local:5432/udi-stub?autoReconnect=true&useSSL=false
  cloud:
    vault:
      kv:
        enabled: false
      authentication: TOKEN
      host: vault.adeo.no
      port: 443
      fail-fast: true
      connection-timeout: 15000
      read-timeout: 30000
      database:
        backend: postgresql/preprod-fss
        role: udi-stub-admin
        enabled: true
  flyway:
    enabled: false
  
  config.import: vault://