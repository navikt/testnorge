spring:
  cloud:
    vault:
      database:
        enabled: false
      token: "test"
      enabled: false
  datasource:
    url: jdbc:tc:postgresql:14.4:///test # 14.4 er versjon p.t. i dev-gcp.
    username: user
    password: pass
  jpa:
    hibernate:
      ddl-auto: create
  flyway:
    enabled: false
