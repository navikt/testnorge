spring:
  cloud:
    vault:
      database:
        enabled: false
      token: "test"
      enabled: false
  datasource:
    url: jdbc:tc:postgresql:15.7:///test?TC_REUSABLE=true # 15.7 er versjon p.t. i dev-gcp.
    username: user
    password: pass
  jpa:
    hibernate:
      ddl-auto: create
  flyway:
    enabled: false

TOKEN_X_ISSUER: dummy