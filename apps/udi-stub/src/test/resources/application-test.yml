TOKEN_X_ISSUER: dummy

spring:
  cloud:
    gcp:
      secretmanager:
        enabled: false
    vault:
      enabled: false
  datasource:
    url: jdbc:tc:postgresql:15:///test?TC_REUSABLE=true
    username: user
    password: pass
  jpa:
    hibernate:
      ddl-auto: create
  flyway:
    enabled: false