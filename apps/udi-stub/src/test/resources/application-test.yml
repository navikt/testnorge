spring:
  datasource:
    driver-class-name: org.testcontainers.jdbc.ContainerDatabaseDriver # TODO: Remove once Spring Boot > 2.3.
    url: jdbc:tc:postgresql:14.4:///dollyDB # 14.4 er versjon p.t. i dev-gcp.
    username: sa
    password:
  flyway:
    enabled: true
    url: jdbc:tc:postgresql:14.4:///dollyDB # 14.4 er versjon p.t. i dev-gcp.
    user: sa
    password: