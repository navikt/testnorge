apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: testnav-inntektsmelding-service-dev
  namespace: dolly
  labels:
    team: dolly
spec:
  tokenx:
    enabled: true
  azure:
    application:
      allowAllUsers: true
      enabled: true
      tenant: nav.no
  accessPolicy:
    inbound:
      rules:
        - application: team-dolly-lokal-app
          cluster: dev-gcp
        - application: testnav-oversikt-frontend
          cluster: dev-gcp
        - application: dolly-backend-dev
          cluster: dev-gcp
    outbound:
      rules:
        - application: testnav-inntektsmelding-generator-service
        - application: logging
          namespace: nais-system
      external:
        - host: testnav-inntektsmelding-generator-service.intern.dev.nav.no
        - host: testnav-dolly-proxy.dev-fss-pub.nais.io
  env:
    - name: SPRING_PROFILES_ACTIVE
      value: dev
  image: "{{image}}"
  port: 8080
  replicas:
    min: 1
    max: 1
  resources:
    requests:
      cpu: 20m
      memory: 600Mi
    limits:
      memory: 1024Mi
  ingresses:
    - "https://testnav-inntektsmelding-service-dev.intern.dev.nav.no"
  liveness:
    path: /internal/health/liveness
    periodSeconds: 30
    initialDelay: 30
  readiness:
    path: /internal/health/readiness
    initialDelay: 30
  observability:
    logging:
      destinations:
        - id: elastic
        - id: loki
    autoInstrumentation:
      enabled: true
      runtime: java
  prometheus:
    enabled: true
    path: /internal/metrics
