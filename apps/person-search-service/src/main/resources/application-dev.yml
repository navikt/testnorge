azure.app.client.id: ${client_id}
azure.app.client.secret: ${client_secret}

elasticsearch:
  client:
    host: testnav-pdl-proxy.dev.intern.nav.no
    port: 443

spring:
  data:
    elasticsearch:
      client:
        reactive:
          endpoints: ${elasticsearch.client.host}/pdl-elastic:${elasticsearch.client.port}
    