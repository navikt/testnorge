spring:
  cloud:
    vault:
      token: dummy
      enabled: false
      database:
        enabled: false
    gcp:
      secretmanager:
        enabled: false
  datasource:
    url: jdbc:tc:postgresql:15:///test_database
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: create

azure:
  app:
    client:
      id: dummy
      secret: dummy
