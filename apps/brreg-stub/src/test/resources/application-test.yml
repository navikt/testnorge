spring:
  cloud:
    vault:
      token: dummy
      enabled: false
      database:
        enabled: false
  datasource:
    url: jdbc:tc:postgresql:15.7:///test_database # 15.7 er versjon p.t. i dev-gcp.
    username: user
    password: password
  jpa:
    hibernate:
      ddl-auto: create

azure:
  app:
    client:
      id: dummy
      secret: dummy
