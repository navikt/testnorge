
spring:
  flyway:
    locations: "classpath:db/migration/postgresql"
  main.banner-mode: "off"
  datasource:
    url: "jdbc:postgresql://b27dbvl032.preprod.local:5432/testnav-brregstub?autoReconnect=true&useSSL=false"
    hikari:
      maximum-pool-size: 2
      connection-test-query: "select 1"
      connection-timeout: 30000
      initialization-fail-timeout: 10000
      minimum-idle: 1
      jdbc-url: "jdbc:postgresql://b27dbvl032.preprod.local:5432/testnav-brregstub?autoReconnect=true&useSSL=false"
    jpa:
      hibernate:
        ddl-auto: none
      properties:
        hibernate:
          dialect: org.hibernate.dialect.PostgreSQLDialect
  jpa:
    open-in-view: false
  cloud:
    vault:
      host: vault.adeo.no
      port: 443
      connection-timeout: 15000
      read-timeout: 30000
      kv:
        enabled: false
      database:
        enabled: true
        role: testnav-brregstub-admin
        backend: postgresql/preprod-fss
      fail-fast: true
  config:
    import: vault://

server:
  servlet:
    encoding:
      charset: UTF-8
