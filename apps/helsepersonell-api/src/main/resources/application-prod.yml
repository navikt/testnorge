AAD_ISSUER_URI: https://login.microsoftonline.com/62366534-1ec3-4962-8869-9b5535279d0b
CLIENT_ID: ${azure.app.client.id}

avspillingsgruppe.helsepersonell.id: ${AVSPILLERGRUPPE_HELSEPERSONELL}

samhandlerregisteret.api.url: ${SAMHANDLERREGISTERET_URL}

hodejegeren.api.url: ${HODEJEGEREN_URL}

helsepersonell.controller.cache.hours: ${CACHE_HOURS_HELSEPERSONELL}

spring:
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${AAD_ISSUER_URI}/v2.0
          accepted-audience: ${CLIENT_ID}, api://${CLIENT_ID}