avspillingsgruppe.leger.id=${AVSPILLERGRUPPE_LEGER}

samhandlerregisteret.api.url=${SAMHANDLERREGISTERET_URL}

hodejegeren.api.url=${HODEJEGEREN_URL}

helsepersonell.controller.leger.cache.hours=${CACHE_HOURS_LEGER}