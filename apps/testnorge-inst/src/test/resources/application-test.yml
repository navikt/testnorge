consumers:
  synt-inst-gcp:
    url: http://localhost:${wiremock.server.port:0}/synt-inst
    cluster: dummy
    namespace: dummy
    name: synthdata-inst-gcp-test
  testnorge-hodejegeren:
    url: http://localhost:${wiremock.server.port:0}/hodejegeren/api
  inst2-web:
    url: http://localhost:${wiremock.server.port:0}/inst2/web/api
  inst2-new:
    url: http://localhost:${wiremock.server.port:0}/inst2/api

ida:
  username: dummy
  password: dummy

azure:
  app:
    client:
      id: dummy
      secret: dummy

spring:
  cloud:
    vault:
      enabled: false