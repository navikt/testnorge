apiVersion: "nais.io/v1alpha1"
kind: "Application"
metadata:
  name: testnorge-ident-service
  namespace: dolly
  labels:
    team: dolly
spec:
  image: {{ image }}
  port: 8080
  webproxy: true
  azure:
    application:
      enabled: true
  liveness:
    path: /internal/isAlive
    initialDelay: 4
    periodSeconds: 5
    failureThreshold: 500
  readiness:
    path: /internal/isReady
    initialDelay: 4
    periodSeconds: 5
    failureThreshold: 500
  replicas:
    min: 1
    max: 1
  resources:
    requests:
      cpu: 200m
      memory: 4000Mi
    limits:
      cpu: 500m
      memory: 5000Mi
  ingresses:
    - "https://testnorge-ident-service.dev.adeo.no"
  vault:
    enabled: true
    paths:
      - kvPath: /kv/preprod/fss/testnorge-ident-service/default
        mountPath: /var/run/secrets/nais.io/vault
